# typescript-course
result school-2023: module_typescript 
1. Since an object is passed as a function parameter, we type this object using the interface. 
2. The function returns the types number or string, so we indicate this.
3. We write logic to calculate the cost taking into account discounts and installments.
